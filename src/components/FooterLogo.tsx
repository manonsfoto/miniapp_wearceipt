import LogoIcon from "../assets/SVG/LogoIcon";

const FooterLogo = () => {
  return (
    <div className="flex flex-col items-center">
      <LogoIcon />
      <p className="font-Modak-Regular text-2xl capitalize -mt-2 tracking-wide">
        Wearceipt
      </p>
    </div>
  );
};

export default FooterLogo;
